<%= form_with(model: identidade) do |form| %>
  <% if identidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(identidade.errors.count, "error") %> prohibited this identidade from being saved:</h2>

      <ul>
        <% identidade.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :grr %>
    <%= form.text_field :grr %>
  </div>

  <div class="field">
    <%= form.label :cpf %>
    <%= form.text_field :cpf %>
  </div>

  <div class="field">
    <%= form.label :discente_id %>
    <%= form.collection_select :discente_id, Discente.order(:nome), :id, :nome %>
  </div>

  <div class="actions">
    <%= form.submit 'Criar Identidade', class: 'view-button' %>
  </div>
<% end %>
